# =====================================================
# Selling Sisters - Environment Variables
# =====================================================
# Copy this file to .env.local and fill in the values
# DO NOT commit .env.local to git!
# =====================================================

# Vercel KV (auto-configured by Vercel when you add KV storage)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Vercel Blob (auto-configured by Vercel when you add Blob storage)
BLOB_READ_WRITE_TOKEN=

# Email Service (Resend - get your API key from https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxx

# Content Publishing Webhook Secret
# Generate a secure random string (e.g., using: openssl rand -hex 32)
# This must match the secret in your Google Apps Script
CONTENT_PUBLISH_SECRET=your-secure-secret-here

# App Configuration
VITE_APP_URL=http://localhost:3000
ORDER_EMAIL_RECIPIENT=DylanKDelagarza@gmail.com
